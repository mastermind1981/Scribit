<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:textSize="28sp"
        android:layout_marginBottom="15dp"
        android:id="@+id/loc_refresh_prompt_tv"
        android:text="Yikes! Looks like you didn't have location tracking enabled. If you just enabled it, tap here to refresh. Otherwise, go back whence you came."
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ImageView
        android:src="@drawable/ic_refresh"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/loc_refresh_prompt_tv"
        android:layout_width="100dp"
        android:layout_height="100dp" />

</RelativeLayout>